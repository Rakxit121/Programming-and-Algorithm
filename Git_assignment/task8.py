# Write a Python program to convert a tuple to a string.

name = ('r', 'a', 'k', 's', 'h', 'i', 't')
# str.join allows adding items of Tuples to an empty string converting it into a string
string = "".join(tuple)

print(string)
print(type(string))
